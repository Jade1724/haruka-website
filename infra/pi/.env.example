# Copy to .env in this same directory (compose variable interpolation).

POSTGRES_PASSWORD=<placeholder>
# Cloudflare remotely-managed tunnel token (Zero Trust dashboard -> Tunnels).
CLOUDFLARE_TUNNEL_TOKEN=<placeholder>
